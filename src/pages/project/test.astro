---
import Layout from "../../layouts/Layout.astro";

/* 
    TODO:
        - Read from local json file, typed by project detail
        - Get dynamic route working -- build static paths
        - Use paths params to get static props
        - Route from tile click
*/

---

<Layout title="Project Title">
    <main>
        <div class="main-container">
            <div class="inner-container">
                <h1 class="project-header">Title here</h1>
                <div class="img-container">
                    <img src="/test-placeholder.jpg" alt="placeholder.jpg" width="100%" height="auto" />
                </div>
                <div class="project-text">
                    <p>content goes here</p>
                </div>
            </div>
        </div>
    </main>
</Layout>

<style lang="scss">
@import "../../styles/abstracts.scss";

main {
    background-color: color("primary");
}

.project-text {
    color: color("secondary-light")
}

.project-header {
    color: white;
}

.img-container {
    text-align: center;
    width: 95%;
}

.main-container {
    display: flex;
    flex-direction: column;
    min-height: calc(100vh - 3rem);
    padding: 1rem;
    max-width: 1100px;
    margin: 0 auto;
}

.inner-container {
    @include flex-box(column, wrap, flex-start, center);
    background-color: color("primary-light");
    height: 100%;
    width: 100%;
    flex-grow: 1;
}

@media screen and (min-width: screen("sm")) {
    .img-container {
        width: 90%
    }

    .main-container {
        min-height: calc(100vh - 6rem);
        padding: 2rem;
    }
}

@media screen and (min-width: screen("md")) {
    .img-container {
        width: 75%
    }
    .main-container {
        min-height: calc(100vh - 9rem);
        padding: 3rem;
    }
}

@media screen and (min-width: screen("lg")) {
    .img-container {
        width: 50%;
    }
    .main-container {
        min-height: calc(100vh - 15rem);
        padding: 5rem;
    }
}
</style>